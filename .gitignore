# Raw dataset (large; GitHub file-size limits, and README says extend/download separately)
transactions.csv
identity.csv
closed_cases_history.csv
case_pack.csv
*.pdf
data/raw/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Generated / scratch
data/processed/
data/vectors/
*.log

# Isolated worktrees (subagent-driven-development)
.worktrees/

# Task 14 batch output is committed as the actual submission (cases/,
# runs/latest/) -- see runs/latest/PROVENANCE.md for how the final 20 were
# assembled. cases_backup/ and runs_backup/ stay ignored: scratch output from
# intermediate Ollama runs during consolidation, superseded by cases/.
cases_backup/
runs_backup/
